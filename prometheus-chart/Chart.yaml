apiVersion: v2
name: prometheus
description: Single Prometheus server, with an optional json-exporter that turns the Morpheus /api/servers stats into metrics
type: application
version: 2.0.0
appVersion: "3.5.0"
