replicaCount: 3

image:
  repository: webdevops/php-nginx
  tag: "8.3-alpine"
  pullPolicy: IfNotPresent

service:
  type: LoadBalancer
  port: 80
  nodePort: 30081

resources:
  limits:
    cpu: 100m
    memory: 128Mi
  requests:
    cpu: 100m
    memory: 128Mi

configMap:
  name: web-dashboard-config
