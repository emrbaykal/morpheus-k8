apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ template "voting-app.fullname" . }}-db-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.database.storage.size }}
  storageClassName: {{ .Values.database.storage.classname }}